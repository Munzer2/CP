#include<bits/stdc++.h>
#define ll long long
#define all(x) x.begin(), x.end()
#define pb(x) push_back(x)
using namespace std;

void debug_vector(vector<ll>& a) {
    for (auto x : a) cout << x << " ";
    cout << "\n";
    return;
}

void solve() {
    ll n ; 
    cin >> n ; 
    string s; 
    cin >> s;
    ll _z = -1,cnt = 0;
    ll f = 0 ; 
    ll ans = 0; 
    for(ll i = 1 ;i  <= n; ++i) {
        
    }
    cout << ans << "\n"; 
    return;
}

int main() {
    ll tt;
    cin >> tt;
    while (tt--) {
        solve();
    }
    return 0;
}